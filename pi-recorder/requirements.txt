# pi-recorder Python dependencies

# GPIO control (Raspberry Pi)
gpiozero>=2.0

# ePaper display (Waveshare)
# Install from: https://github.com/waveshare/e-Paper
# After cloning the repo, run:
#   pip install ./e-Paper/RaspberryPi_JetsonNano/python/
# Or install the community package:
waveshare-epaper>=1.3.0

# Image generation for display rendering
Pillow>=10.0.0

# Optional: richer system stats (falls back to /proc parsing if absent)
psutil>=5.9.0

# Note: ffmpeg is a system binary, not a Python package.
# Install with: sudo apt install ffmpeg v4l-utils
